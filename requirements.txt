# Wash Friends RAG System — Python Dependencies
# Install: pip install -r requirements.txt --break-system-packages

# Core AI
anthropic>=0.34.0          # Claude API
chromadb>=0.5.0            # Vector database

# Embeddings (choose one)
openai>=1.40.0             # For OpenAI text-embedding-3-small (recommended)
# sentence-transformers>=3.0.0  # Local fallback (no API cost, but slower)

# Web server (Zalo webhook)
fastapi>=0.112.0
uvicorn[standard]>=0.30.0

# HTTP client
httpx>=0.27.0

# Utilities
python-dotenv>=1.0.0       # .env file loading
